import { aspCurrencyMaskDirective } from './asp-currency-mask.directive';

describe('aspCurrencyMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new aspCurrencyMaskDirective();
    expect(directive).toBeTruthy();
  });
});
