import { aspMaskDirective } from './asp-mask.directive';

describe('aspMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new aspMaskDirective();
    expect(directive).toBeTruthy();
  });
});
