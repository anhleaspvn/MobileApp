import { InputMaxMinDirective } from './input-max-min.directive';

describe('InputMaxMinDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaxMinDirective();
    expect(directive).toBeTruthy();
  });
});
