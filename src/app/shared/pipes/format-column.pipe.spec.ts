import { FormatColumnPipe } from './format-column.pipe';

describe('FormatColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
