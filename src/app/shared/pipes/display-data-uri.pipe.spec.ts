import { DisplayDataUriPipe } from './display-data-uri.pipe';

describe('DisplayDataUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDataUriPipe();
    expect(pipe).toBeTruthy();
  });
});
